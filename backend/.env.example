# Backend Environment Variables

# Server Port
PORT=5000

# Node Environment (development, production)
NODE_ENV=production

# MongoDB Atlas URI - Replace with your connection string
# IMPORTANT: If your password contains special characters (@, :, #, etc.), URL-encode them
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/quickshare?retryWrites=true&w=majority

# Frontend URL for CORS (comma-separated for multiple origins)
FRONTEND_URL=https://your-frontend-domain.com

# Cloudinary Configuration (for file uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# File upload limits (in bytes) - 500MB default
MAX_FILE_SIZE=524288000

# Admin credentials (optional)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_admin_password
# Discord Bot (optional - for uploading files and text via Discord)
# Create a bot at https://discord.com/developers/applications
# Required intents: MESSAGE_CONTENT, GUILDS, GUILD_MESSAGES
DISCORD_BOT_TOKEN=your_discord_bot_token

# Set to 'true' to enable. Requires Chrome installed on server.
# Optional: Custom path to Chrome executable (auto-detected on Windows)
